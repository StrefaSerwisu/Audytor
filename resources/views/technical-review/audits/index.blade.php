<x-auditor.layout title="Weryfikacja techniczna 2.0"><div class="page-head"><div><h1>Weryfikacja techniczna 2.0</h1><p class="muted">Audyty przekazane przez inzynierow.</p></div></div><div class="stack">@forelse($audits as $audit)<a class="card" href="{{ route('technical-review.audits.show',$audit) }}"><h2>{{ $audit->number }}</h2><strong>{{ $audit->client->name }}</strong><div class="meta"><span class="pill">{{ $audit->status->label() }}</span><span class="pill">{{ $audit->progress_percent }}%</span></div></a>@empty<div class="card muted">Brak audytow do weryfikacji.</div>@endforelse</div></x-auditor.layout>
