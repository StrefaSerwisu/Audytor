<x-auditor.layout :title="$audit->number"><div class="stack"><a href="{{ route('technical-review.audits.index') }}">Wroc</a><h1>{{ $audit->number }}</h1><strong>{{ $audit->client->name }}</strong></div>@if($errors->any())<div class="error">{{ $errors->first() }}</div>@endif<div class="stack" style="margin-top:18px">@foreach($audit->modules as $module)<section class="card"><h2>{{ $module->name }}</h2>@foreach($module->controls as $control)<article class="card" style="box-shadow:none"><div class="meta"><strong>{{ $control->code }} {{ $control->name }}</strong><span class="pill">{{ $control->status }}</span></div><div class="grid"><div><strong>Odpowiedz</strong><div>{{ json_encode($control->answer?->value_json,JSON_UNESCAPED_UNICODE) }}</div></div><div><strong>Wynik</strong><div>{{ $control->answer?->result_status }}</div></div><div><strong>Ryzyko</strong><div>{{ $control->answer?->proposed_risk_level }}</div></div><div><strong>Rekomendacja</strong><div>{{ $control->answer?->proposed_recommendation }}</div></div><div><strong>Deklaracja klienta</strong><div>{{ $control->answer?->customer_statement_source }}</div></div><div><strong>Dowody</strong><div>{{ $control->evidence->count() }}</div></div></div><form class="grid" method="post" action="{{ route('technical-review.controls.update',[$audit,$control]) }}">@csrf @method('patch')<label>Komentarz<input name="comment"></label><label>Ryzyko<input name="proposed_risk_level" value="{{ $control->answer?->proposed_risk_level }}"></label><label>Rekomendacja<input name="proposed_recommendation" value="{{ $control->answer?->proposed_recommendation }}"></label><label class="inline-check"><input type="checkbox" name="changes_requested" value="1">Do poprawy</label><button>Zapisz</button></form></article>@endforeach</section>@endforeach</div><section class="card" style="margin-top:18px"><div class="meta"><form method="post" action="{{ route('technical-review.audits.transition',[$audit,'changes_requested']) }}">@csrf<input name="comment" required placeholder="Komentarz"><button>Zwroc do poprawy</button></form><form method="post" action="{{ route('technical-review.audits.transition',[$audit,'technically_approved']) }}">@csrf<button>Zatwierdz technicznie</button></form></div></section></x-auditor.layout>
