<x-auditor.layout title="Eskalacje"><div class="page-head"><div><h1>Eskalacje techniczne</h1><p class="muted">Pytania i blokady wymagajace konsultacji.</p></div></div><div class="stack">@forelse($escalations as $e)<article class="card"><div class="meta"><span class="pill">{{ $e->priority }}</span><span class="pill">{{ $e->status }}</span><a href="{{ route('engineer.controls.show',[$e->audit,$e->control]) }}">{{ $e->audit->number }} / {{ $e->control->code }}</a></div><strong>{{ $e->reason }}</strong><div>{{ $e->question }}</div>@if(auth()->user()->can('respond',$e))<form class="stack" method="post" action="{{ route('engineer.escalations.respond',$e) }}">@csrf @method('patch')<label>Odpowiedz<textarea name="response" required>{{ $e->response }}</textarea></label><label class="inline-check"><input type="checkbox" name="resolve" value="1">Rozwiaz eskalacje</label><button>Wyślij odpowiedz</button></form>@elseif($e->response)<div class="notice">{{ $e->response }}</div>@endif</article>@empty<div class="card muted">Brak eskalacji.</div>@endforelse</div></x-auditor.layout>
