<x-auditor.layout title="Audyty inzyniera 2.0"><div class="page-head"><div><h1>Audyty inzyniera 2.0</h1><p class="muted">Techniczna realizacja zlecen Delivery.</p></div><a class="button" href="{{ route('engineer.escalations.index') }}">Eskalacje</a></div><form class="card grid" method="get"><label>Status<select name="status"><option value="">Wszystkie</option>@foreach($statuses as $v=>$l)<option value="{{ $v }}" @selected(($filters['status']??'')===$v)>{{ $l }}</option>@endforeach</select></label><label>Klient<select name="client_id"><option value="">Wszyscy</option>@foreach($clients as $c)<option value="{{ $c->id }}">{{ $c->name }}</option>@endforeach</select></label><label class="inline-check"><input type="checkbox" name="mine" value="1">Tylko moje</label><label class="inline-check"><input type="checkbox" name="blocked" value="1">Z blokadami</label><label class="inline-check"><input type="checkbox" name="escalated" value="1">Z eskalacjami</label><button>Filtruj</button></form><div class="stack" style="margin-top:18px;">@forelse($audits as $audit)<a class="card" href="{{ route('engineer.audits.show',$audit) }}"><div class="page-head" style="margin:0"><div><h2>{{ $audit->number }}</h2><strong>{{ $audit->client->name }} / {{ $audit->auditType->name }}</strong></div><span class="pill {{ $audit->status->color() }}">{{ $audit->status->label() }}</span></div><div class="progress"><span style="--progress:{{ $audit->progress_percent }}%"></span></div><div class="meta"><span class="pill">{{ $audit->progress_percent }}%</span><span class="pill">Blokady: {{ $audit->blocked_controls }}</span><span class="pill">Eskalacje: {{ $audit->escalated_controls }}</span><span class="pill">{{ $audit->order->planned_start_at?->format('Y-m-d H:i') }}</span></div></a>@empty<div class="card muted">Brak audytow.</div>@endforelse</div></x-auditor.layout>
